{"seq": 1, "category": "testing", "severity": "major", "confidence": "high", "file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/commands/PersonalKeyHandler.java", "lines": [63, 99], "snippet": "    public void doReissue(Player sender, int keyId) {\n        ApiKey key = apiKeyService.getKey(keyId);\n        if (key == null || !\"PERSONAL\".equals(key.getKeyType())) {\n            message.send(sender, \"treasuryapi.personal.reissue.not-found\");\n            return;\n        }\n        if (!key.getOwnerUuid().equals(sender.getUniqueId())) {\n            message.send(sender, \"treasuryapi.personal.reissue.no-access\");", "description": "PersonalKeyHandler has zero tests, yet it contains security-relevant authorization logic: the owner-UUID equality gate on doReissue/doRevoke, the key-type discrimination (rejecting a BUSINESS key id passed to a personal command), the null-key not-found branch, and the terminal-revoked pre-check (ADT-110). Every collaborator (ApiKeyService, TreasuryApi, Message) is an injected interface and Player is trivially mockable/fakeable \u2014 the class is fully unit-testable in the same hand-rolled-fake style already used by ApiKeyServiceImplTest, so nothing structural blocks coverage. A regression that dropped the owner check (letting any player rotate/revoke another player's key) or inverted the type check would ship silently.", "recommendation": "Add a PersonalKeyHandlerTest with a fake ApiKeyService and a stub Message recorder: assert doReissue/doRevoke send the no-access key when the sender is not the owner, the not-found key when getKey returns null or a BUSINESS-typed key, the revoked key when the key is revoked, and that reissueKey/revokeKey are invoked only on the success path. Verify each test fails if the corresponding guard is deleted.", "effort": "medium", "occurrences": [], "id": "treasury-api-plugin/testing/0001", "component": "treasury-api-plugin", "dimension": "testing", "verification": "confirmed", "verification_note": "Snippet matches PersonalKeyHandler.java:63-72 verbatim. doReissue/doRevoke both contain security-relevant gates: null/type check (line 65,88 rejecting non-PERSONAL keys) and the owner-UUID equality gate (line 69,92 key.getOwnerUuid().equals(sender.getUniqueId())), plus the terminal-revoked pre-check (line 76, ADT-110). No test in src/test references PersonalKeyHandler (grep -rl empty). All collaborators (ApiKeyService, TreasuryApi, Message) are injected interfaces and ApiKey is a Lombok @Data model with getKeyType/getOwnerUuid/isRevoked, so it is fully unit-testable in the same anonymous-fake style ApiKeyServiceImplTest already uses (new ApiKeyMapper(){...}). A regression dropping the owner check would let any player rotate/revoke another player's key and ship silently. Major is fair for an untested authorization gate on a security-sensitive command surface."}
{"seq": 2, "category": "testing", "severity": "major", "confidence": "high", "file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/commands/BusinessKeyHandler.java", "lines": [131, 138], "snippet": "    private boolean canManage(ApiKey key, Player sender) {\n        return key.getFirmId() != null\n                && businessApi.firms().isProprietor(key.getFirmId(), sender.getUniqueId());\n    }", "description": "BusinessKeyHandler is untested. Its canManage gate encodes an explicit audit fix (ADT-111): reissue/revoke of a firm-scoped BUSINESS key must be gated on CURRENT proprietorship of the key's firm, not the original issuer. Also untested: the firm-not-found and non-proprietor branches on doIssue, the type/null discrimination in doReissue/doRevoke, and the terminal-revoked pre-check (ADT-110). BusinessApi and ApiKeyService are injected interfaces; Player and Firm are mockable \u2014 the handler is fully unit-testable with fakes. A regression that reverted canManage to an issuer-identity check, or dropped the firmId!=null null-guard (NPE on a personal key mis-typed), would go undetected.", "recommendation": "Add a BusinessKeyHandlerTest with a fake BusinessApi (firms().getFirm/isProprietor) and fake ApiKeyService: assert issue rejects non-proprietors and unknown firms, reissue/revoke reject a non-current-proprietor, canManage returns false when firmId is null, and the revoked pre-check fires. Confirm the tests fail if canManage's isProprietor call is replaced by an issuer==sender check.", "effort": "medium", "occurrences": [], "id": "treasury-api-plugin/testing/0002", "component": "treasury-api-plugin", "dimension": "testing", "verification": "unverified"}
{"seq": 3, "category": "testing", "severity": "major", "confidence": "high", "file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/mappers/ApiKeyMapper.java", "lines": [46, 89], "snippet": "    @Update(\"\"\"\n            UPDATE api_keys\n               SET jwt_id     = #{jwtId},\n                   issued_at  = #{issuedAt},\n                   expires_at = #{expiresAt}\n             WHERE key_id = #{keyId}\n               AND revoked = 0\n            \"\"\")\n    int reissue(@Param(\"keyId\") int keyId,", "description": "None of the four mappers (ApiKeyMapper, ExplorerGroupMapper, ExplorerUiMapper, UuidBinaryTypeHandler) has any integration test, while sibling plugins treasury and business run their mappers against a real MariaDB (MariadbContainerExtension) and treasury-rest-api uses EmbeddedMariaDb. The SQL invariants here are load-bearing and only asserted via in-memory fakes in ApiKeyServiceImplTest, never against a real dialect: the `AND revoked = 0` reissue guard that makes revocation terminal (ADT-110, and which must stay behaviour-equivalent to treasury-rest-api's rotateKey), the DISTINCT firm_employee JOIN with is_current=1 in findBusinessAccessibleByEmployee, the INSERT IGNORE / ON DUPLICATE KEY upserts, the BINARY(16) UUID round-trip via UuidBinaryTypeHandler, and the useGeneratedKeys keyId back-fill. A SQL typo or a schema drift in any of these would compile and pass the current suite.", "recommendation": "Add a mapper integration test using the same MariaDB harness the sibling plugins already use (MariadbContainerExtension / the economy-flyway schema). Cover at minimum: reissue returns 0 rows for a revoked key and 1 for an active key; findBusinessAccessibleByEmployee returns keys only for is_current=1 employment; the UuidBinaryTypeHandler round-trips a UUID. Do not substitute H2 \u2014 the production dialect is MariaDB and INSERT IGNORE / ON DUPLICATE KEY / BINARY semantics differ.", "effort": "large", "occurrences": [{"file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/mappers/ExplorerGroupMapper.java", "lines": [30, 36], "snippet": "    @Insert(\"INSERT IGNORE INTO explorer_group_member (group_id, player_uuid_bin, source) \"\n            + \"VALUES (#{groupId}, #{uuid}, 'luckperms')\")"}, {"file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/mappers/ExplorerUiMapper.java", "lines": [37, 41], "snippet": "    @Insert(\"INSERT INTO explorer_identity (keycloak_sub, player_uuid_bin, minecraft_name, linked_by) \" +\n            \"VALUES (#{sub}, #{uuid}, #{name}, #{linkedBy}) \" +\n            \"ON DUPLICATE KEY UPDATE player_uuid_bin = VALUES(player_uuid_bin), minecraft_name = VALUES(minecraft_name)\")"}, {"file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/mappers/typehandlers/UuidBinaryTypeHandler.java", "lines": [19, 22], "snippet": "    public void setNonNullParameter(PreparedStatement ps, int i, UUID parameter, JdbcType jdbcType) throws SQLException {\n        ps.setBytes(i, UuidBin.toBytes(parameter));\n    }"}], "id": "treasury-api-plugin/testing/0003", "component": "treasury-api-plugin", "dimension": "testing", "verification": "unverified"}
{"seq": 4, "category": "testing", "severity": "major", "confidence": "high", "file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/tasks/GroupReconciliationTask.java", "lines": [80, 91], "snippet": "    private void apply(int groupId, Set<UUID> desired) {\n        Set<UUID> current = new HashSet<>(mapper.listLuckpermsMemberUuids(groupId));\n        // Guard against a degraded/misconfigured LuckPerms resolving to zero members,\n        // which would otherwise mass-revoke a whole group's synced access in one tick.\n        if (desired.isEmpty() && !current.isEmpty()) {\n            log.warning(\"Reconciliation of explorer group \" + groupId\n                    + \" skipped removals: node resolved to zero members while \"", "description": "GroupReconciliationTask's only test covers the pure nodeKey() helper. The safety-critical mass-revoke guard in apply() \u2014 refuse to prune when the desired set is empty but current members exist (the documented 'never mass-revoke on a transient empty result' invariant) \u2014 has no pinning test, and neither does matcherFor() (the group-vs-permission node interpretation, a sibling of the tested nodeKey with the same branch structure). apply() is private and only reachable through run(), which calls the LuckPerms-bound resolveMembers(), so as written the guard is not unit-testable without a live LuckPerms; that packaging is itself the reason the highest-risk branch here is unverified.", "recommendation": "Extract the desired-vs-current diff-and-apply step (including the empty-desired guard) into a package-private method taking (int groupId, Set<UUID> desired) plus the injected ExplorerGroupMapper, and pin it: empty desired + non-empty current performs no removes; empty desired + empty current is a no-op; a normal diff adds/removes the expected UUIDs. Also add matcherFor() unit assertions mirroring the existing nodeKey tests (bare name and group.<x> -> InheritanceNode; dotted -> PermissionNode).", "effort": "medium", "occurrences": [{"file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/tasks/GroupReconciliationTask.java", "lines": [198, 206], "snippet": "    static NodeMatcher<? extends Node> matcherFor(String node) {\n        if (node.startsWith(\"group.\")) {\n            return NodeMatcher.key(InheritanceNode.builder(node.substring(\"group.\".length())).build());\n        }"}], "id": "treasury-api-plugin/testing/0004", "component": "treasury-api-plugin", "dimension": "testing", "verification": "unverified"}
{"seq": 5, "category": "testing", "severity": "minor", "confidence": "high", "file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/commands/UiAccessHandler.java", "lines": [70, 91], "snippet": "        OfflinePlayer target = Bukkit.getOfflinePlayer(playerName);\n        if (!target.hasPlayedBefore() && !target.isOnline()) {\n            message.send(sender, \"treasuryapi.ui.user.unknown-player\", \"player\", playerName);\n            return;\n        }", "description": "UiAccessHandler is untested and, unlike the other two handlers, is partly wired to hidden Bukkit statics (Bukkit.getOfflinePlayer in doUserAdd/resolveUuid, Bukkit.getPluginManager in doSelfSync). That static coupling is a framework-seam smell: the unknown-player guard (ADT-38, refusing to grant a privileged role to a fabricated offline UUID) and the invalid-role rejection cannot be exercised in a plain unit test without a Bukkit mock, so a regression in either security guard is untestable as structured. The role-validation set membership itself (VALID_ROLES) is pure and could be tested if the name->UUID resolution were behind an injected seam.", "recommendation": "Introduce a small injected PlayerResolver seam (name -> Optional<UUID>/played-before) so doUserAdd's unknown-player guard and doUserRemove/doUserList are unit-testable without Bukkit statics, then add a UiAccessHandlerTest covering: invalid role rejected before any DB write, unknown player rejected, valid grant writes the role and logs the audit line, remove reports granted vs not-granted from the mapper's affected-row count.", "effort": "medium", "occurrences": [], "id": "treasury-api-plugin/testing/0005", "component": "treasury-api-plugin", "dimension": "testing", "verification": "unverified"}
{"seq": 6, "category": "testing", "severity": "minor", "confidence": "medium", "file": "treasury-api-plugin/src/main/java/io/paradaux/treasuryapi/services/KeycloakAdminClient.java", "lines": [52, 54], "snippet": "    private ReentrantLock lockFor(String sub) {\n        return subjectLocks[Math.floorMod(sub.hashCode(), LOCK_STRIPES)];\n    }", "description": "KeycloakAdminClient has no tests. Two pieces are testable without a live Keycloak: the per-subject lock-stripe selection lockFor (ADT-113, which must be deterministic per sub and always in-bounds \u2014 Math.floorMod guards the negative-hashCode case that a raw % would not) and the env-var-precedence secret selection in serviceAccountToken (TREASURYAPI_KEYCLOAK_CLIENT_SECRET overriding config, ADT-38). Neither is pinned, so a regression reintroducing a raw % (negative index -> ArrayIndexOutOfBounds for subjects with a negative hashCode) or dropping the env-var precedence would not be caught.", "recommendation": "Make lockFor package-private (or add a stripeIndex(String) helper) and assert it returns the same in-range index for the same sub and handles a subject string whose hashCode is negative. If the secret-selection logic is extracted to a package-private helper, pin the env-var-wins-over-config precedence.", "effort": "small", "occurrences": [], "id": "treasury-api-plugin/testing/0006", "component": "treasury-api-plugin", "dimension": "testing", "verification": "confirmed", "verification_note": "Snippet matches KeycloakAdminClient.java:52-54 verbatim. Math.floorMod(sub.hashCode(), LOCK_STRIPES) is the deterministic in-bounds lock-stripe selection (a raw % would yield a negative index for negative hashCodes -> ArrayIndexOutOfBounds), and serviceAccountToken (lines 109-112) implements the TREASURYAPI_KEYCLOAK_CLIENT_SECRET env-var-over-config precedence (ADT-38). No test references KeycloakAdminClient. Both behaviors are pure/testable without a live Keycloak. Minor is fair: it is a testing gap on non-authorization helper logic with small blast radius, not a security gate."}
{"seq": 7, "category": "testing", "severity": "minor", "confidence": "high", "file": "treasury-api-plugin/src/test/java/io/paradaux/treasuryapi/services/impl/ApiKeyServiceImplTest.java", "lines": [110, 124], "snippet": "        assertThrows(IllegalStateException.class,\n                () -> new ApiKeyServiceImpl(mapperReturning(active, 0), config).reissueKey(6));\n    }\n\n    /** In-memory mapper whose findById returns {@code found} and reissue affects {@code rows}. */\n    private static ApiKeyMapper mapperReturning(ApiKey found, int rows) {", "description": "ApiKeyServiceImplTest is solid on the JWT mint and reissue-guard paths but leaves two service methods with no behavioural assertion: revokeKey (only ever wired as a fake returning 0 or 1, never asserted to delegate to mapper.revoke) and the two list delegators listKeys/listBusinessKeysAccessibleByEmployee (fakes return List.of(), and no test asserts the service passes through the owner/type arguments or the returned list). These are thin delegators, so the gap is low-risk, but they are testable behaviour with no lock. Also note the reissue tests assert only that an exception is thrown, not that no token was minted/returned on the 0-rows path \u2014 a mild under-assertion of the 'never mint an unusable token' intent.", "recommendation": "Add assertions that revokeKey invokes mapper.revoke with the given keyId, and that listKeys/listBusinessKeysAccessibleByEmployee return exactly the mapper's list and forward the ownerUuid/keyType arguments. Optionally strengthen the 0-rows reissue test to assert the exception message rather than only the type.", "effort": "small", "occurrences": [], "id": "treasury-api-plugin/testing/0007", "component": "treasury-api-plugin", "dimension": "testing", "verification": "unverified"}

{"seq": 1, "category": "testing/coverage-gap", "severity": "major", "confidence": "high", "file": "lib/util/ssrf.ts", "lines": [42, 73], "snippet": "export function isBlockedAddress(ip: string): boolean {\n  const v = net.isIP(ip);\n  if (v === 4) return isBlockedV4(ip);\n  if (v === 6) return isBlockedV6(ip);\n  return true;\n}", "description": "lib/util/ssrf.ts is the SSRF guard for player-supplied webhook URLs (the first line of defence against a player pointing a webhook at 127.0.0.1, 169.254.169.254 cloud metadata, RFC1918, CGNAT, IPv6 loopback/ULA, or ::ffff: mapped-v4 addresses). isBlockedAddress/isBlockedV4/isBlockedV6/assertPublicHttpsUrl are pure functions with no server-only runtime dependency beyond node:dns and node:net, yet there is not a single test referencing them (grep for 'ssrf|isBlockedAddress|assertPublicHttpsUrl' across test/ returns nothing). This is security-critical boundary logic with many hand-coded ranges (e.g. 100.64/10 CGNAT, 172.16-31, the ::ffff: v4-mapped branch) \u2014 exactly the class of code where an off-by-one or a missed range silently opens an SSRF hole, and it has zero regression lock.", "recommendation": "Add a unit test that drives isBlockedAddress across each blocked range boundary (0.x, 10.x, 127.x, 169.254.x, 172.15/172.16/172.31/172.32, 192.168.x, 100.63/100.64/100.128, 224.x, ::1, ::, fe80::, fc00::, fd00::, ff02::, ::ffff:127.0.0.1, ::ffff:8.8.8.8) plus one public v4 and one public v6 that must pass, and assertPublicHttpsUrl rejecting http/, credentialed, and unresolvable hosts. Stub node:dns lookup to test the resolution path.", "effort": "small", "occurrences": [{"file": "lib/util/ssrf.ts", "lines": [12, 39], "snippet": "export async function assertPublicHttpsUrl(raw: string): Promise<URL> {"}, {"file": "lib/util/ssrf.ts", "lines": [49, 62], "snippet": "function isBlockedV4(ip: string): boolean {"}], "id": "economy-explorer/testing/0001", "component": "economy-explorer", "dimension": "testing", "verification": "confirmed", "verification_note": "Snippet at ssrf.ts:42-47 matches verbatim. grep -riE 'ssrf|isBlockedAddress|assertPublicHttpsUrl|isBlockedV4|isBlockedV6' over test/ returns nothing (exit 1). The file is security-critical SSRF boundary logic with hand-coded ranges (100.64/10 CGNAT line 59, 172.16-31 line 57, ::ffff: v4-mapped branch line 70-71, ::1/:: line 66). isBlockedV4/isBlockedV6 are pure \u2014 no server-only/dns dependency, trivially unit-testable \u2014 yet have zero regression lock, exactly the class of code where a missed range silently opens an SSRF hole. Major is justified for untested security boundary logic."}
{"seq": 2, "category": "testing/coverage-gap", "severity": "minor", "confidence": "high", "file": "lib/util/discord.ts", "lines": [9, 17], "snippet": "export function isDiscordWebhookUrl(raw: string): boolean {\n  let u: URL;\n  try {\n    u = new URL(raw.trim());\n  } catch {\n    return false;\n  }\n  return HOSTS.has(u.hostname.toLowerCase()) && PATH.test(u.pathname);\n}", "description": "isDiscordWebhookUrl is pure (explicitly documented as having no server-only deps) and mirrors the dispatcher's DiscordWebhook.java host/path allowlist \u2014 a classic drift-prone mirror pair. It has no test (grep 'discord|isDiscordWebhookUrl' across test/ finds only an unrelated schema column). The host set (discord.com, discordapp.com, canary/ptb subdomains) and the path regex /^\\/api(\\/v\\d+)?\\/webhooks\\/\\d+\\/.+/ are untested, so a typo or a divergence from the Java side would not be caught.", "recommendation": "Add a small unit test asserting true for each allowlisted host with a valid /api/webhooks/<id>/<token> and /api/v10/webhooks/... path, and false for a non-Discord host, a bad path, and a non-URL string.", "effort": "trivial", "occurrences": [], "id": "economy-explorer/testing/0002", "component": "economy-explorer", "dimension": "testing", "verification": "unverified"}
{"seq": 3, "category": "testing/coverage-gap", "severity": "minor", "confidence": "high", "file": "lib/services/group.ts", "lines": [95, 101], "snippet": "export async function resolvePlayerUuid(input: string): Promise<string | null> {\n  const trimmed = input.trim();\n  if (UUID_RE.test(trimmed)) {\n    return trimmed.toLowerCase();\n  }\n  return findPlayerUuidByName(trimmed);\n}", "description": "resolvePlayerUuid contains the only non-trivial pure logic in the group service \u2014 trimming, UUID-shape detection, and lower-casing a canonical UUID before falling through to a name lookup. It gates admin-typed member identifiers into the RBAC group. It has no unit test (grep 'resolvePlayerUuid|services/group' across test/ finds nothing). The UUID-vs-name branch and the toLowerCase normalisation are testable without a DB by injecting/stubbing findPlayerUuidByName, but the module reaches the DAL directly so it needs a light mock; the trim+regex+lowercase path can be asserted directly.", "recommendation": "Add a unit test for resolvePlayerUuid: a mixed-case canonical UUID returns lower-cased verbatim without hitting the DAL; a non-UUID string is trimmed and delegated to findPlayerUuidByName (stub it); whitespace is stripped. setGroupCapabilities's transactional replace is better left to an integration test.", "effort": "small", "occurrences": [], "id": "economy-explorer/testing/0003", "component": "economy-explorer", "dimension": "testing", "verification": "unverified"}
{"seq": 4, "category": "testing/coverage-gap", "severity": "minor", "confidence": "high", "file": "lib/auth/requireRole.ts", "lines": [17, 22], "snippet": "export function requireRole(viewer: Viewer, min: RequiredRole): asserts viewer is Extract<Viewer, { anon: false }> {\n  if (viewer.anon) throw new UnauthorizedError(`This page requires sign-in.`);\n  if (RANK[viewer.role === 'player' ? 'player' : viewer.role] < RANK[min]) {\n    throw new ForbiddenError(`This page requires ${min} access.`);\n  }\n}", "description": "requireRole is the tier gate for /me, /admin/*, /government \u2014 a permission-edge behaviour the charter calls out for pinning tests. It is pure (only depends on the Viewer type and error classes) and trivially unit-testable, yet untested (the only 'requireRole' reference in test/ is a comment). Its RANK comparison with the `viewer.role === 'player' ? 'player' : viewer.role` normalisation is a boundary-prone expression (a viewer whose role is 'player' vs a legacy/unknown role) that has no regression lock. requireAdmin (lib/auth/requireAdmin.ts) has the same status: pure, gates every admin server action, no test.", "recommendation": "Add a unit test matrix: anon throws UnauthorizedError; player meets 'player' but is rejected for 'government'/'admin' with ForbiddenError; government meets 'government' but not 'admin'; admin meets all three. Do the same for requireAdmin (admin passes; player/government/anon throw ForbiddenError).", "effort": "small", "occurrences": [{"file": "lib/auth/requireAdmin.ts", "lines": [11, 15], "snippet": "export function requireAdmin(viewer: Viewer): asserts viewer is Extract<Viewer, { anon: false }> {\n  if (viewer.anon || viewer.role !== 'admin') {\n    throw new ForbiddenError('Admin only.');\n  }\n}"}], "id": "economy-explorer/testing/0004", "component": "economy-explorer", "dimension": "testing", "verification": "unverified"}
{"seq": 5, "category": "testing/fixture-drift", "severity": "major", "confidence": "high", "file": "test/integration/schema.sql", "lines": [1, 20], "snippet": "SET FOREIGN_KEY_CHECKS=0;\n\nCREATE TABLE `accounts` (\n  `account_id` int(10) unsigned NOT NULL AUTO_INCREMENT,\n  `account_type` enum('PERSONAL','BUSINESS','GOVERNMENT','SYSTEM') NOT NULL,", "description": "The integration suite (test/integration/db.ts loads test/integration/schema.sql) runs the real lib/sql queries against a real MariaDB \u2014 good, no H2 dialect mismatch. But the DDL it runs is a 364-line hand-maintained snapshot, NOT the authoritative economy-flyway migrations. The schema-drift CI gate (economy-explorer-schema-drift.yml) only regenerates and diffs lib/db.generated.ts against a Flyway-migrated DB; it never touches test/integration/schema.sql (grep confirms no reference). So a Flyway column rename/drop/added-NOT-NULL can leave schema.sql stale while the integration tests keep passing green against the old shape \u2014 the very drift class ADT-5 set out to prevent, but for the integration fixture rather than the TS types. The tests then assert behaviour that no longer matches production DDL.", "recommendation": "Either run the integration tests against a Flyway-migrated database (apply economy-flyway migrations to the CI MariaDB service, drop the checked-in schema.sql), or add a drift check that diffs a mysqldump of the Flyway-migrated schema against test/integration/schema.sql and fails on divergence. The first is preferable \u2014 it removes the snapshot entirely.", "effort": "medium", "occurrences": [], "id": "economy-explorer/testing/0005", "component": "economy-explorer", "dimension": "testing", "verification": "unverified"}
{"seq": 6, "category": "testing/config", "severity": "minor", "confidence": "high", "file": "vitest.config.mts", "lines": [15, 20], "snippet": "    coverage: {\n      provider: 'v8',\n      reporter: ['text', 'text-summary', 'html', 'lcov'],\n      include: ['lib/**/*.ts'],\n      exclude: ['lib/**/*.d.ts', 'lib/db.ts', 'lib/auth/authjs.ts'],\n    },", "description": "CI runs `npm run coverage` (vitest run --coverage) on every PR, but the coverage block declares no thresholds (grep for 'threshold|thresholds|lines|branches' in vitest.config.mts returns nothing). Coverage is therefore reported but never enforced \u2014 the run cannot fail on a coverage regression, unlike the JVM modules which have jacoco line/branch gates wired into `check` (treasury/business 0.95, chestshop 0.99). New untested code (e.g. the ssrf/discord/requireRole gaps above) can land without any signal. The `coverage` script and CI step give a false impression of a gate that does not exist.", "recommendation": "Add a coverage.thresholds block (e.g. lines/functions at the current measured baseline) so the CI coverage step fails on regression and ratchets upward, matching the repo's stated coverage-gate convention.", "effort": "trivial", "occurrences": [], "id": "economy-explorer/testing/0006", "component": "economy-explorer", "dimension": "testing", "verification": "confirmed", "verification_note": "Snippet at vitest.config.mts:17-22 matches verbatim. grep for 'threshold|thresholds|lines|branches' returns nothing (exit 1). Confirmed .github/workflows/economy-explorer-ci.yml:65 runs 'npm run coverage' (= 'vitest run --coverage' per package.json) on every PR, so coverage is reported but never enforced \u2014 the run cannot fail on regression, unlike the JVM jacoco gates (treasury-test.yml min-coverage-overall 95). The ssrf gap in seq 1 could land unsignalled. Minor is fair since it is a missing-gate/false-impression issue, not an active defect."}
{"seq": 7, "category": "testing/coverage-gap", "severity": "nit", "confidence": "high", "file": "lib/format.ts", "lines": [31, 46], "snippet": "export function fmtDate(d: string): string {\n  return new Date(d.length === 10 ? d + 'T00:00:00Z' : d).toLocaleDateString('en-US', {\n    month: 'short',\n    day: 'numeric',\n  });\n}\n\nexport function fmtTs(ts: string | Date | null): string {\n  if (!ts) return '\u2014';", "description": "format.test.ts covers looksLikeUuid, accountLabel, shortenUuid, fmtN, fmtAmt, fmtAmtFull, fmtPct thoroughly, but fmtDate and fmtTs are exported and untested. fmtDate has a real behavioural branch \u2014 a 10-char date string gets a 'T00:00:00Z' suffix appended (to force UTC parsing and avoid the local-timezone off-by-one-day bug), while longer strings are passed through. That date-boundary branch is exactly the kind of regression-prone logic the charter flags, and fmtTs's null\u2192'\u2014' fallback is untestable-by-inspection only. Separately, CURRENCY_SYMBOL (lib/currency.ts) \u2014 which every fmtAmt output depends on \u2014 is asserted only with the default '$'; the \u00a3 (StateCraft) tenant override path is never exercised.", "recommendation": "Add cases for fmtDate('2026-05-01') vs fmtDate('2026-05-01T10:00:00Z') pinning the UTC-suffix branch, fmtTs(null) \u2192 '\u2014' and fmtTs of a Date/string, and a fmtAmt test under a stubbed CURRENCY_SYMBOL='\u00a3' to lock the per-tenant symbol path.", "effort": "trivial", "occurrences": [{"file": "lib/currency.ts", "lines": [6, 9], "snippet": "export const CURRENCY_SYMBOL: string =\n  process.env.CURRENCY_SYMBOL && process.env.CURRENCY_SYMBOL.trim().length > 0\n    ? process.env.CURRENCY_SYMBOL.trim()\n    : '$';"}], "id": "economy-explorer/testing/0007", "component": "economy-explorer", "dimension": "testing", "verification": "unverified"}

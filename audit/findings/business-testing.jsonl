{"seq": 1, "category": "testing", "severity": "minor", "confidence": "high", "file": "business/src/main/java/io/paradaux/business/chat/FirmChatService.java", "lines": [37, 37], "snippet": "public class FirmChatService {", "description": "FirmChatService has zero tests (no test file references it anywhere under business/src/test). It owns real, privacy/moderation-critical logic that is trivially unit-testable without Bukkit statics: toggleGlobalSpy / toggleFirmSpy / isSpyingOn / isAnySpy (chat-spy semantics \u2014 global vs per-firm, add/remove toggle, empty-set cleanup at lines 145-178), the recipients() routing that dedups a spy who is also a tuned-in member and prevents cross-firm chat bleed (lines 109-143), forget() session cleanup (line 218), inMultipleFirms (line 225) and firmName (line 181). These methods depend only on the injected FirmService / FirmStaffService and a Business (for its logger) \u2014 all mockable. A regression here silently leaks firm-chat across firms or lets a de-toggled spy keep hearing, with no test to catch it.", "recommendation": "Add a FirmChatServiceImplTest (plain Mockito) covering: toggle round-trips (add then remove returns false / clears the map entry), isSpyingOn honouring globalSpies over firmSpies and null firmId, isAnySpy, forget() clearing all three maps, and recipients() excluding members whose active firm differs and de-duplicating a member who is also a spy. Mock FirmStaffService.getOnlineEmployees to return mocked Players.", "effort": "medium", "occurrences": [], "id": "business/testing/0001", "component": "business", "dimension": "testing", "verification": "downgraded", "original_severity": "major", "verification_note": "Snippet 'public class FirmChatService {' matches verbatim at line 37. Confirmed zero tests: grep -rl FirmChatService over business/src/test returns nothing, and the module has a *Test for nearly every other service (30+ test files), so this is a genuine gap in an otherwise thoroughly-tested, 0.95-line-gated module. The spy-semantics logic is real and non-trivial: toggleFirmSpy (154-164) has empty-set cleanup, recipients() (109-131) dedups a spy who is also a tuned-in member via a UUID-keyed LinkedHashMap and gates each firm's membership on activeFirm to prevent cross-firm bleed. Most cited methods (toggleGlobalSpy/toggleFirmSpy/isSpyingOn/isAnySpy at 145-178, forget at 218, inMultipleFirms at 225, firmName at 181) truly depend only on injected FirmService/FirmStaffService plus in-memory sets and are Bukkit-free/mockable, so the testability claim mostly holds. However the finding overstates it: recipients() and its helper addOnline (134-142) call Bukkit.getPlayer (a Bukkit static) and iterate staff.getOnlineEmployees returning Bukkit Players, so that method is NOT 'trivially unit-testable without Bukkit statics.' More importantly this is a pure test-absence finding: no actual defect in the code is demonstrated, only that it is untested. Per the repo's audit standard test-depth is somewhat deferred pre-prod, so a coverage gap with no shown bug does not warrant 'major'; it is a legitimate but minor coverage gap."}
{"seq": 2, "category": "testing", "severity": "major", "confidence": "high", "file": "business/src/main/java/io/paradaux/business/Business.java", "lines": [128, 128], "snippet": "        injector.getInstance(CommandManager.class).registerAll();", "description": "No startup-shaped test builds the Guice injector (HiberniaModule + BusinessModule + DatabaseModule) and calls registerAll() on CommandManager/ListenerManager (Business.java lines 121-140). A grep of the entire test tree for Guice.createInjector / registerAll / BusinessModule / HiberniaModule returns nothing. The charter requires such a test so that command route conflicts, unbound ParameterResolvers, and missing/mistyped message keys fail in CI rather than only at server enable. Because message keys are referenced as bare string literals (e.g. message.send(sender, \"business.firm.create.success\", ...)), a typo'd or removed key currently has no compile-time or test-time guard \u2014 it only surfaces when a player runs the command in production.", "recommendation": "Add a wiring/smoke test that constructs the injector with the real modules (Treasury APIs mocked) and invokes CommandManager.registerAll() + ListenerManager.registerAll(), asserting no exception. If the framework exposes the registered command/route set and the loaded Message bundle, additionally assert every message key referenced by command handlers resolves against messages.properties, so route conflicts and missing keys break the build.", "effort": "medium", "occurrences": [{"file": "business/src/main/java/io/paradaux/business/Business.java", "lines": [140, 140], "snippet": "        injector.getInstance(ListenerManager.class).registerAll();"}], "id": "business/testing/0002", "component": "business", "dimension": "testing", "verification": "unverified"}
{"seq": 3, "category": "testing", "severity": "minor", "confidence": "high", "file": "business/src/test/java/io/paradaux/business/services/impl/FirmBalanceTaxServiceImplTest.java", "lines": [162, 189], "snippet": "    void multipleAccounts_splitProportionally_withDriftFoldedIntoLargest() {\n        when(firmService.listAllActiveFirms()).thenReturn(List.of(firm(1)));\n        when(firmAccountService.listAccountIds(1)).thenReturn(List.of(10, 11, 12));\n        // Balances chosen so the proportional split leaves a rounding remainder.\n        when(treasury.getBalancesByIds(List.of(10, 11, 12))).thenReturn(Map.of(\n                10, new BigDecimal(\"100.00\"), 11, new BigDecimal(\"100.00\"), 12, new BigDecimal(\"100.00\")));\n        // total 300 * 0.01 = 3.00; per-account 1.00 each, no drift here but exercises the loop.", "description": "The test named withDriftFoldedIntoLargest does NOT exercise the drift-fold-into-largest branch it claims to cover. It uses equal balances 100/100/100 at rate 0.01, so each account's proportional tax is exactly 1.00 and the drift = totalTax - allocated is zero \u2014 the branch `if (drift.signum() != 0 && largestAccountId != null) perAccountTax.merge(largestAccountId, drift, ...)` in FirmBalanceTaxServiceImpl (buildFirmCollections) is never taken. The test's own comment admits 'no drift here'. This is the exact rounding/boundary behaviour the regression-lock charter wants pinned (per-account HALF_UP setScale(2) leaving a remainder that must be folded so the per-account sum equals totalTax); it is currently unverified, so a regression that drops or misapplies the drift correction (currency created/destroyed by a rounding cent) would not be caught.", "recommendation": "Add a case with unequal or non-evenly-divisible balances that produces a real remainder \u2014 e.g. balances 100/100/100 taxed at a total that doesn't divide by 3 (or two accounts 10.00 / 20.00 at a rate whose per-account HALF_UP amounts don't sum to totalTax) \u2014 and assert (a) the per-account amounts sum EXACTLY to totalTax, and (b) the drift cent lands on the largest-balance account specifically.", "effort": "small", "occurrences": [], "id": "business/testing/0003", "component": "business", "dimension": "testing", "verification": "unverified"}
{"seq": 4, "category": "testing", "severity": "minor", "confidence": "high", "file": "business/src/main/java/io/paradaux/business/commands/resolvers/FirmPlayerResolver.java", "lines": [40, 58], "snippet": "    public Optional<FirmPlayer> resolve(String token, CommandSender sender) {\n        if (token == null || token.isBlank()) return Optional.empty();\n\n        // 1) UUID exact\n        UUID asUuid = tryParseUuid(token);\n        if (asUuid != null) {\n            return players.findByUuid(asUuid);\n        }", "description": "The three custom ParameterResolvers have no tests (no test file references FirmPlayerResolver, FirmNameResolver, or OnlineFirmNameResolver). These are pure/injectable and directly testable, and FirmPlayerResolver.resolve carries non-trivial branching the charter singles out: blank->empty, UUID-exact path, compact 32-char UUID re-hyphenation in tryParseUuid (lines 76-92), exact-name match, and prefix search that only resolves when unambiguous (matches.size()==1) \u2014 the ambiguity/no-match edge returns empty. None of these paths, nor the suggestions() distinct/last-known-name mapping, are exercised. FirmNameResolver/OnlineFirmNameResolver.suggestions (player vs console fallback, online-player snapshot union) are likewise untested.", "recommendation": "Add resolver unit tests with a mocked FirmPlayerService/FirmSuggestionCache: assert compact-32-char UUID resolves via findByUuid, an ambiguous 2-match prefix returns empty while a unique prefix resolves, exact-name takes precedence over prefix, and suggestions() dedups current names. For the name resolvers, assert player senders suggest from playerFirmNames and console falls back to activeFirmNames.", "effort": "small", "occurrences": [{"file": "business/src/main/java/io/paradaux/business/commands/resolvers/FirmNameResolver.java", "lines": [43, 49], "snippet": "    public List<String> suggestions(String prefix, CommandSender sender) {\n        if (sender instanceof Player p) {\n            return Suggestions.match(cache.playerFirmNames(p.getUniqueId()), prefix, 20);\n        }\n        return Suggestions.match(cache.activeFirmNames(), prefix, 20);\n    }"}, {"file": "business/src/main/java/io/paradaux/business/commands/resolvers/OnlineFirmNameResolver.java", "lines": [47, 55], "snippet": "    public List<String> suggestions(String prefix, CommandSender sender) {\n        Set<String> pool = new LinkedHashSet<>();\n        // Suggestions run on a Netty thread; snapshot the online players first so a\n        // concurrent join/quit can't throw a ConcurrentModificationException mid-iteration.\n        for (Player online : List.copyOf(Bukkit.getOnlinePlayers())) {\n            pool.addAll(cache.playerFirmNames(online.getUniqueId()));\n        }\n        return Suggestions.match(pool, prefix, 20);\n    }"}], "id": "business/testing/0004", "component": "business", "dimension": "testing", "verification": "unverified"}

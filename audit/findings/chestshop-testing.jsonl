{"seq": 1, "category": "testing", "severity": "minor", "confidence": "high", "file": "chestshop/src/test/java/io/paradaux/chestshop/services/impl/TransactionServiceImplTest.java", "lines": [2016, 2018], "snippet": "            service.validate(pending(BUY, false, sign(location(world)), client, owner, mock(Inventory.class),\n                    new ItemStack[]{item(Material.STONE, 1)}, new BigDecimal(\"5\")));\n            Thread.sleep(1100); // let the cooldown window lapse", "description": "The notification-cooldown regression test asserts the owner is re-notified after the cooldown lapses by sleeping 1100ms of wall-clock time against a 1-second config window. This is a real-time-dependent concurrency/timing test \u2014 it adds >1.1s to the suite and is inherently flaky under CI load (a stalled JVM between the two validate() calls could push either side of the boundary). The root cause is in the SUT: TransactionServiceImpl.sendError (chestshop/src/main/java/io/paradaux/chestshop/services/impl/TransactionServiceImpl.java:523,526) reads System.currentTimeMillis() directly with no injectable Clock/time source, so the cooldown boundary cannot be exercised deterministically. This is the exact Thread.sleep-based-timing anti-pattern the charter flags.", "recommendation": "Introduce an injectable time source (e.g. a LongSupplier or java.time.Clock constructor-injected into TransactionServiceImpl) and replace the two System.currentTimeMillis() reads at TransactionServiceImpl.java:523 and :526 with it. Then drive the cooldown boundary in the test by advancing the fake clock instead of Thread.sleep(1100), removing the wall-clock dependency and the 1.1s runtime cost.", "effort": "medium", "occurrences": [], "id": "chestshop/testing/0001", "component": "chestshop", "dimension": "testing", "verification": "downgraded", "original_severity": "major", "verification_note": "Snippet matches verbatim at TransactionServiceImplTest.java:2016-2018. The test sendError_ownerNotification_resendsAfterCooldownExpires does Thread.sleep(1100) against a 1-second cooldown (config.getNotificationMessageCooldown()=1L) and asserts sendMessage is called twice. Root cause confirmed: TransactionServiceImpl.sendMessageToOwner reads System.currentTimeMillis() directly at lines 524 and 527 (the finding's 523/526 are off by one but point at the correct block) with no injectable Clock/time source, so notificationCooldowns (a HashBasedTable of raw millis) cannot be advanced deterministically. The test is genuinely real-time-dependent, adds >1.1s to the suite, and is flaky (only a 100ms margin over the 1s window, so a GC/scheduler stall between the two validate() calls can cross the boundary). All factual claims hold. Severity downgraded to minor: this is an isolated test-quality/flakiness defect on a single test method with a bounded blast radius (an occasional CI flake and ~1s cost), not a production correctness bug and not a repeated pattern; major overstates the impact."}
{"seq": 2, "category": "testing", "severity": "major", "confidence": "high", "file": "chestshop/src/main/java/io/paradaux/chestshop/ChestShop.java", "lines": [144, 156], "snippet": "        injector.getInstance(io.paradaux.hibernia.framework.commander.CommandManager.class).registerAll();\n...\n        injector.getInstance(io.paradaux.hibernia.framework.events.ListenerManager.class).registerAll();", "description": "No startup-shaped test exists that builds the Guice module (ChestShopModule) and calls CommandManager.registerAll()/ListenerManager.registerAll(). Grep across chestshop/src/test/** for Guice.createInjector, new ChestShopModule, CommandManager, ListenerManager, and registerAll returns zero hits. registerAll() is invoked only in production ChestShop.java. There are 11 annotation-driven @Command/@Route command classes; without a wiring smoke test, a duplicate route, a missing Guice binding, or a message key referenced by a command handler but absent from messages.properties will not fail in CI \u2014 it will surface only at server boot. The charter explicitly requires verifying such a startup test exists. MockBukkit is already available and used by 26 tests, so a headless boot test is feasible.", "recommendation": "Add a startup test that constructs the injector with ChestShopModule (over a MockBukkit server), resolves CommandManager and ListenerManager, and calls registerAll() on each, asserting no exception and no duplicate-route/missing-binding error. This pins route conflicts, missing bindings, and message-key gaps into CI.", "effort": "medium", "occurrences": [], "id": "chestshop/testing/0002", "component": "chestshop", "dimension": "testing", "verification": "unverified"}
{"seq": 3, "category": "testing", "severity": "minor", "confidence": "high", "file": "chestshop/src/main/java/io/paradaux/chestshop/mappers/typehandlers/UuidStringTypeHandler.java", "lines": [41, 43], "snippet": "    private static UUID toUuid(String value) {\n        return value == null ? null : UUID.fromString(value);\n    }", "description": "The two custom MyBatis TypeHandlers (UuidStringTypeHandler, DateLongTypeHandler) and the two mappers (AccountMapper with 10 SQL statements, ItemCodeMapper with 8) are entirely untested. All service tests substitute a hand-written FakeAccountMapper / fake mapper, so the real @Select/@Insert SQL strings and the TypeHandler conversions are never exercised. The mappers/** and typehandlers are also excluded from the jacoco coverage gate (build.gradle.kts jacocoExcludes includes \"io/paradaux/chestshop/mappers/**\"), so the 0.99/0.99 gate provides no signal here. The TypeHandlers are pure (they take java.sql.PreparedStatement/ResultSet interfaces, no Bukkit statics) and the null-handling round-trip is directly unit-testable; the mapper SQL runs against a local SQLite file, which can be spun up in-process in a test (no Testcontainers/H2 dialect-mismatch concern since production is SQLite). A malformed SQL string or a TypeHandler regression would ship undetected.", "recommendation": "Add direct unit tests for UuidStringTypeHandler and DateLongTypeHandler (mock ResultSet/PreparedStatement, assert the string/long round-trip and null handling). Add an integration test that opens an in-process SQLite database, applies the mappers' createTable DDL, and round-trips AccountMapper/ItemCodeMapper CRUD so the annotation SQL is validated against the real dialect.", "effort": "medium", "occurrences": [{"file": "chestshop/src/main/java/io/paradaux/chestshop/mappers/typehandlers/DateLongTypeHandler.java", "lines": [1, 1], "snippet": "package io.paradaux.chestshop.mappers.typehandlers;"}, {"file": "chestshop/src/main/java/io/paradaux/chestshop/mappers/AccountMapper.java", "lines": [54, 54], "snippet": "    @Select(\"SELECT \" + COLUMNS + \" FROM accounts WHERE uuid = #{uuid} ORDER BY lastSeen DESC LIMIT 1\")"}, {"file": "chestshop/src/main/java/io/paradaux/chestshop/mappers/ItemCodeMapper.java", "lines": [1, 1], "snippet": "package io.paradaux.chestshop.mappers;"}], "id": "chestshop/testing/0003", "component": "chestshop", "dimension": "testing", "verification": "unverified"}
